#Duplicating workspace
워크스페이스에서 작업한 코드를 git hub에 업로드 할때 node_modules는 올릴 필요가 없다.
대신 셋업을 저장한 package.json이 필요하다.
따라서 package.json이 있으면 npm install로 필요한 셋업을 저장할 수 있다.

#Project 시작하기
먼저 컴포넌트들과 Hierarchy를 디자인 한 다음 각 컴포넌트를 위한 jsx파일을 각각 만든다.
